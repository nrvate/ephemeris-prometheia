# Ephemeris Prometheia — design record

*Prometheia* (προμήθεια) — Greek for **forethought**, the quality Prometheus
personifies. The library stores forethought (initial conditions) and computes
answers by integration, instead of storing answers baked years in advance.

This document is the project's decision record: what was decided, why, and
what was rejected. It grows with the project; dated entries below are binding
unless superseded in place.

## The paradigm

Ephemeris libraries in the Swiss-Ephemeris tradition are **bake-then-read**:
positions are precomputed offline, packed into per-object files, and the
library decompresses and evaluates polynomials. The cost model is
**file size ∝ objects × time-span** — which is why the numbered-asteroid
catalog became tens of gigabytes across thousands of files, and why
asteroids were second-class citizens of the API.

Prometheia inverts this: a catalog stores **initial conditions** (osculating
elements + uncertainties at one epoch) and the library carries a
production-grade integrator. Positions are computed on demand and memoized.
The cost model becomes **file size ∝ objects**; the time axis is free, and
accuracy degrades *honestly* (propagated sigma) instead of silently.

This is not a speculative architecture: it is how the reference data itself
is produced. JPL's DE ephemerides *are* integrations; every baked ephemeris
file in existence is a frozen derivative of one. Swiss Ephemeris itself
already integrates fictitious bodies from osculating elements at query time
— the paradigm it reserves for the bodies its file format couldn't carry.

## Evidence from the Swiss Ephemeris source (read 2026-09-16, v2.10.03-ts.10)

Findings that motivated specific decisions here (analysis of their source;
no code read or reused — see Cleanroom policy):

| SWE behavior | Consequence | Our decision |
|---|---|---|
| All numbered asteroids share **one** open-file slot (`SEI_NEPHFILES = 7`); interleaved requests thrash open/close/free | Slow multi-object scans | Catalog stack with single container, indexed random access, no file juggling |
| Up to 1,000,000 files in 1,000 `astN/` subdirectories; identity encoded in the *path* | O(original sin) operational mess; names require opening files or scanning a 16 MB text list per lookup | Identity is data (spkid + pool strings); name resolution is an index lookup |
| `.se1` coefficients are fixed-point ints scaled by a global `rmax`; per-segment size-group headers | Precision coupled to a global constant; format has no checked-in generator | f64 elements from the authoritative source; format generated by tools in this repo |
| Files split per 600-year period; asteroid files hard-code −3000..+3000 | Time span baked into file inventory | No time axis in records at all |
| Per-context state is 34.8 KB of hand-maintained memo caches with hit rates annotated in comments | Maintainers fighting their own architecture | Caching designed in (chunk cache now; integrator memo in M2), instrumented (`Reader::stats()`) |
| Silent ephemeris substitution (Moshier fallback) unless patched | Quiet precision loss that callers cannot detect | Honest answers: `NotFound`/`FormatError` instead of substitution; propagated uncertainty instead of false confidence |

## Decisions

1. **Paradigm: store initial conditions, integrate on demand.** Memoize
   integrated windows; steady-state evaluation cost reaches parity with
   baked-file evaluation (~100 ns class); cold cost is a one-time
   millisecond-class integration per object per window. **Measured
   2026-09-16** (`prometheia-bench`, 100-body real-SBDB fixture, Jupiter +
   Saturn perturbers, DP5(4) increment): cold 79 bodies ±1 yr = 10.6 ms
   total (0.134 ms/body); warm memoized evaluation = 18 ns; full 100-body
   10-yr arc = 69 ms. Full-catalog ±1 yr extrapolates to ~3.5 min/core.
   Re-measure at every integrator increment; these numbers gate docs.
2. **Planetary provider: read JPL DE binaries directly (v1).** The DE binary
   format is public, documented JPL material; we write our own cleanroom
   reader. Planets/Moon are the one place baked data has an accuracy
   argument (relativity, lunar theory, asteroid perturbers). The DE reader
   doubles as the truth source for validating everything else.
3. **Compact integrated solar system (v2):** a ~10 KB initial-condition
   block for the Sun + planets + Moon, integrated in-process, validated
   against a Horizons-sampled verification corpus; target ~0.1″ class over
   ±3000 yr. Deferred to keep v1's risk surface small.
4. **Small bodies: integrate from catalog elements.** For the tail, baked
   data has *no* accuracy argument — existing asteroid ephemeris files are
   themselves propagations from older orbit solutions. On-demand integration
   from the freshest SBDB orbit is equal accuracy without the storage.
5. **Data: JPL-only.** SBDB Query API (bulk elements + sigmas + physical
   params), Horizons (validation sampling), DE binaries. MPC data carries
   attribution-only/more restrictive terms; excluding it keeps the data
   story airtight. SBDB's count as of 2026-09-16: **1,564,460 asteroids**,
   3.7k comets.
6. **Format: EPM1** — see [FORMAT.md](FORMAT.md). No Chebyshev, no segments,
   no time axis. CBOR metadata with provenance. Per-chunk zstd + CRC-32.
   The full ingestion pipeline (SBDB request shape, sbdb-raw v1, unit
   conversions, validation, freshness strategy) is specified in
   [INGESTION.md](INGESTION.md).
7. **API: C++20, no process-wide globals, ever.** Contexts are the
   foundation, not a bolt-on (the lesson of the Swiss Ephemeris threading
   forks). C ABI shim for bindings; transport heads (HTTP, binary socket,
   custom protocol) as thin add-on libraries over the same core. M6.
8. **Astrology transforms in v1:** ICRF-native core with ecliptic-of-date,
   topocentric, light-time/aberration, sidereal ayanamsas from published
   formulas. Houses, fictitious bodies, fixed stars: later milestones.
9. **License: GPL-2.0-or-later**, matching Astrolog, the project this
   work supports. (The original sketch said public domain; superseded
   2026-09-16 by explicit owner decision. Data files remain US-government
   public domain as data.)
10. **Platforms: Linux-first CI**, portable C++20 throughout; macOS/Windows/
    Android after the numerical core is validated.
11. **Naming:** repo slug `ephemeris-prometheia`, library `libprometheia`,
    namespace `prometheia`, tools `prometheia-*`, future daemon
    `prometheiad`, container magic `EPM1`.

## Data-source etiquette (binding)

SBDB and Horizons are free public services of JPL/Caltech. Policy for every
tool and script in this repo, and for anyone running them:

- **Strictly sequential requests.** One request in flight; no threads, no
  connection pools. `sbdb_fetch.py` enforces this structurally.
- **Rate-limited:** ≥2 s between page requests (`--delay`), and the more
  patient the bulk pull, the larger the delay should be.
- **Identify yourself:** the fetcher sends a descriptive User-Agent so
  operators can see what is polling and why.
- **Back off on errors** (5, 10, 20, 40 s) — retries never speed up.
- **Small pages by default** in development; full-catalog pulls are
  deliberate, announced runs, not side effects of a build.
- If JPL ever publishes guidance that conflicts with this list, their
  guidance wins; update this file the same day.

## Roadmap

- **M0** — bootstrap: repo, CMake, GPL, CI, docs. *(done)*
- **M1** — EPM1 container + SBDB ingest pipeline + tools. *(done)*
- **M2** — mechanics core. *(done 2026-09-16, three increments)*
  - Increment 1: Kepler engine + adaptive DP5(4) with FSAL (25a1fa6).
  - Increment 2: cubic-Hermite perturber tables, heliocentric force
    model, WindowMemo, benchmark harness (4908ea1).
  - Increment 3: Radau-15, the IAS15-class collocation integrator, with
    constants derived from theory and verified against the paper
    (1ef6d5c).
  - Increment 4 (parked): Everhart's triangular b-sequence in place of
    the monomial Vandermonde solve — better conditioning, fewer
    corrector iterations, lower noise floor.
- **M3** — DE binary reader; time scales (TDB/TT/UTC/UT1, ΔT, leap
  seconds); IAU 2006/2000A precession-nutation; ICRF →
  ecliptic/equatorial/topocentric. Test corpus: de200.eph on hand.
- **M4** — engine API (`prometheia::Engine`), catalog stack overlay,
  calc() with flags/sigma/provenance, pdes→spkid secondary index,
  ayanamsa layer, C ABI shim, `ephem` CLI.
- **M5** — validation gates: Horizons-sampled corpus, per-tier precision
  thresholds, full-catalog bench run (the ~3.5 min/core extrapolation
  must be measured), numeric cross-check against installed Swiss
  Ephemeris as an output-only oracle.
- **M6** — transports: `Transport` interface, binary-socket head
  (length-prefixed CBOR), `prometheiad` HTTP head.

## Cleanroom policy

No Swiss Ephemeris source, headers, or tables are read or referenced during
implementation. SWE's behavior is cited in this document only as observed
*behavior* (from running it and reading its documentation/architecture), the
way one cites any black box. Numerical comparison against SWE output (M5) is
output-only oracle testing — comparing our published answers to theirs —
which is standard cleanroom verification practice. All astronomy comes from
published, citable theory (JPL DE documentation, IAU standards, IAS15 paper,
published ayanamsa definitions).
